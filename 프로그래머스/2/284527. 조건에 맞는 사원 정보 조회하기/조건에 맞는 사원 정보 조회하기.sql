SELECT 
    SUM(B.SCORE) AS SCORE,
    A.EMP_NO,
    A.EMP_NAME,
    A.POSITION,
    A.EMAIL
FROM 
    HR_EMPLOYEES A NATURAL JOIN HR_GRADE B
GROUP BY
    YEAR, EMP_NO
HAVING
    YEAR = 2022
ORDER BY 
    SCORE DESC
LIMIT
    1;
    